# 数据迁移方案

数据来源：`data/import/` 下的 mock CSV。

迁移步骤：

1. 确认字段映射。
2. 保存为 UTF-8 CSV。
3. 执行导入脚本。
4. 查看成功、失败、跳过数量。
5. 查询数据库数量。
6. 抽查客户、产品、订单。

命令：

```bash
python scripts/import_data.py --folder data/import
```

失败行需要修正 CSV 后重新导入，不允许把错误数据包装成成功。
